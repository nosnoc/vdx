clear all
close all

%TODO(@anton) 

stage_counts = [10, 25, 50, 75, 100, 250, 500, 750, 1000];
n_s_list = [1,2,3,4]
N_fe = 2;

% do no FESD runs
no_fesd_errors = [];
for n_s=n_s_list
    for N_stages=stage_counts
        h
    end
end

% do FESD runs
for n_s=n_s_list
    for N_stages=stage_counts
    end
end
